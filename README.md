# Esto es un api de prueba
para arrancar el proyecto se debe ejecutar el siguiente comando:

```bash
npm install
```
luego:

```bash
npm run start
```

en modo desarrollo se ejecuta el siguiente:
```bash
npm run dev
```
